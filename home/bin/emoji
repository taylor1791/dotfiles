#! /usr/bin/env bash
set -euo pipefail

DATA="$(curl -N https://www.emojicopy.com/assets/js/vendor/emoji.json 2> /dev/null)"
EMOJI="$(echo "$DATA" | jq 'keys | .[]' | shuf | head -1)"

echo "$DATA" | jq -r ".$EMOJI.shortname"

