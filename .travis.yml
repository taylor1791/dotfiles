language: c

matrix:
  include:
  - {os: linux, sudo: required, dist: trusty}
  - {os: osx, sudo: required, osx_image: xcode8.3}

script:
- '[[ "$TRAVIS_OS_NAME" == "osx" ]] && bin/test.sh'
- '[[ "$TRAVIS_OS_NAME" == "linux" ]] && docker run -v `pwd`:/root -w /root ubuntu:16.04 /root/bin/test.sh'

