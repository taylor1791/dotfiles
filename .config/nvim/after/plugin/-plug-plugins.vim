" This informs plug there are no more plugins and it can begin loading and
" setting the runtimepath.
call plug#end()

" Note: by starting this filename with a "-", vim will execute it before
" anything starting with /[a-z]/i.
